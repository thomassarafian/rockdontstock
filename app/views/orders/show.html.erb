<div class="container">
  <div class="row py-4">
    <div class="col-md-3"><%= image_tag @order.sneaker.photos[0].url, width: '100%' %></div>
    <div class="col">
      <h1>Order #<%= @order.id %> <small><span class="badge badge-primary"><%= @order.state %></span></small></h1>
      <ul class="list-unstyled">
        <li><strong>ISSUED ON</strong> <%= @order.created_at.to_date %></li>
        <li><strong>sneaker</strong> <%= @order.sneaker.name %></li>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.price %></li>
        </li>
      </ul>
      <p>Estimated delivery in <strong>2 business days</strong></p>
      <%= link_to "Retour à la liste", sneakers_path%>
    </div>
  </div>
</div>
