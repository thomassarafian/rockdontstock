var modalEl = document.getElementById('modal');
var modal = bootstrap.Modal.getOrCreateInstance(modalEl);

modalEl.querySelector('#form-container').innerHTML = "<%= j render partial: 'form_request' %>";

modal.show();
